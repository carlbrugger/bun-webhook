{
  "name": "webhook",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start:watch": "bun run --watch index.ts",
    "start": "bun run index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
